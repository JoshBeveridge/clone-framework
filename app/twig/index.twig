<!DOCTYPE html>
<html lang="en">
    <head>
        <title>Clone: Framework</title>
        <meta charset="utf-8" />
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <link href="https://fonts.googleapis.com/css?family=Euphoria+Script|Open+Sans:400,400i,600,600i,800,800i&amp;subset=latin-ext" rel="stylesheet">
        <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
        <link async rel="stylesheet" href="css/clone.css">
        <link async rel="stylesheet" href="css/clone-gridless.css">
    </head>
    <body data-clone data-c-menu-style="topbar left">
        {# {% include "tests/menu.twig" %} #}
        <div data-c-padding="base(tb, 1) tl(tb, 4)" data-c-alignment="base(center)" data-c-color="base(black)">
            <div data-c-container="base(full, center)">
                <p>Looking for documentation? Check out <a href="https://designwithclone.ca">Clone's Docs</a> for more information.</p>
            </div>
        </div>
        <div data-c-border="base(top, thin, solid, gray)" data-c-padding="base(tb, 4)">
            <div data-c-container="base(medium, center)">
                {# {% include "tests/alert.twig" %} #}
                {% include "tests/accordion.twig" %}
                {# {% include "components/accordion.twig" %} #}
                {# {% include "tests/dialog.twig" %} #}

                {# {% include "components/input/text.twig" %} #}
                {# {% include "components/input/password.twig" %} #}
                {# {% include "components/breadcrumb.twig" %} #}
                {# {% include "components/input/telephone.twig" %} #}
                {# {% include "tests/grid.twig" %} #}
                {# {% include "tests/forms.twig" %} #}
                {# {% include "tests/carousel.twig" %} #}
                {# {% include "tests/headings.twig" %} #}
                {# {% include "tests/buttons.twig" %} #}
            </div>
            {# {% include "tests/container.twig" %} #}
        </div>
        <script src="js/clone.js" type="module"></script>
    </body>
</html>
